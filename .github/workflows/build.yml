name: TqModes VIP Xcode Build
on:
  workflow_dispatch:
    inputs:
      app_name:
        default: 'TqModes'
      web_url:
        default: ''
      use_local_html:
        default: 'false'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Cài đặt XcodeGen
        run: brew install xcodegen

      - name: Tạo Cấu Trúc App Chuẩn
        run: |
          mkdir -p AppSource
          
          cat << 'EOF' > project.yml
          name: TqApp
          options:
            bundleIdPrefix: com.tqmodes
          targets:
            TqApp:
              type: application
              platform: iOS
              deploymentTarget: "14.0"
              sources: [AppSource]
              info:
                path: AppSource/Info.plist
                properties:
                  UILaunchStoryboardName: LaunchScreen
                  NSAppTransportSecurity:
                    NSAllowsArbitraryLoads: true
          EOF

          cat << 'EOF' > AppSource/AppDelegate.swift
          import UIKit
          import WebKit

          @UIApplicationMain
          class AppDelegate: UIResponder, UIApplicationDelegate {
              var window: UIWindow?
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  window = UIWindow(frame: UIScreen.main.bounds)
                  let vc = UIViewController()
                  let webView = WKWebView(frame: UIScreen.main.bounds)
                  webView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
                  webView.scrollView.bounces = false
                  
                  let useLocal = "${{ github.event.inputs.use_local_html }}" == "true"
                  let webUrl = "${{ github.event.inputs.web_url }}"
                  
                  if useLocal {
                      if let htmlPath = Bundle.main.path(forResource: "index", ofType: "html") {
                          let url = URL(fileURLWithPath: htmlPath)
                          webView.loadFileURL(url, allowingReadAccessTo: url.deletingLastPathComponent())
                      } else {
                          webView.loadHTMLString("<h1>Chưa có file index.html</h1>", baseURL: nil)
                      }
                  } else if let url = URL(string: webUrl) {
                      webView.load(URLRequest(url: url))
                  }
                  
                  vc.view.addSubview(webView)
                  window?.rootViewController = vc
                  window?.makeKeyAndVisible()
                  return true
              }
          }
          EOF
          
          cat << 'EOF' > AppSource/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleName</key>
              <string>${{ github.event.inputs.app_name }}</string>
              <key>CFBundleDisplayName</key>
              <string>${{ github.event.inputs.app_name }}</string>
              <key>CFBundleIdentifier</key>
              <string>com.tqmodes.${{ github.run_id }}</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>UILaunchStoryboardName</key>
              <string>LaunchScreen</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
          </dict>
          </plist>
          EOF
          
          cat << 'EOF' > AppSource/LaunchScreen.storyboard
          <?xml version="1.0" encoding="UTF-8"?>
          <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB" version="3.0" toolsVersion="21507" targetRuntime="iOS.CocoaTouch" propertyAccessControl="none" useAutolayout="YES" launchScreen="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="01J-lp-oVM">
              <device id="retina6_12" orientation="portrait" appearance="light"/>
              <dependencies>
                  <deployment identifier="iOS"/>
                  <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="21505"/>
                  <capability name="Safe area layout guides" minToolsVersion="9.0"/>
                  <capability name="documents saved in the Xcode 8 format" minToolsVersion="8.0"/>
              </dependencies>
              <scenes>
                  <scene sceneID="EHf-IW-A2E">
                      <objects>
                          <viewController id="01J-lp-oVM" sceneMemberID="viewController">
                              <view key="view" contentMode="scaleToFill" id="Ze5-6b-2t3">
                                  <rect key="frame" x="0.0" y="0.0" width="393" height="852"/>
                                  <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
                                  <color key="backgroundColor" white="1" alpha="1" colorSpace="custom" customColorSpace="genericGamma22GrayColorSpace"/>
                              </view>
                          </viewController>
                          <placeholder placeholderIdentifier="IBFirstResponder" id="iYj-Kq-Ea1" userLabel="First Responder" sceneMemberID="firstResponder"/>
                      </objects>
                      <point key="canvasLocation" x="53" y="375"/>
                  </scene>
              </scenes>
          </document>
          EOF

          if [ "${{ github.event.inputs.use_local_html }}" == "true" ]; then
            if [ -f "index.html" ]; then
              cp index.html AppSource/
            fi
          fi
          
          xcodegen generate

      - name: Build bằng Xcode (Chuẩn Apple)
        run: |
          # Đã ép SYMROOT để file xuất đúng vào thư mục hiện tại
          xcodebuild -project TqApp.xcodeproj -scheme TqApp -sdk iphoneos -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO SYMROOT=$(pwd)/build build
          
          mkdir -p Payload
          # Kiểm tra chắc chắn file đã xuất ra thành công
          if [ -d "build/Release-iphoneos/TqApp.app" ]; then
            cp -r build/Release-iphoneos/TqApp.app Payload/
            zip -r "${{ github.event.inputs.app_name }}.ipa" Payload/
          else
            echo "❌ Lỗi: Build không thành công, không tìm thấy file TqApp.app"
            exit 1
          fi
          
      - name: Gửi về Telegram
        uses: actions/upload-artifact@v4
        with:
          name: IPA_OUTPUT
          path: ./*.ipa
